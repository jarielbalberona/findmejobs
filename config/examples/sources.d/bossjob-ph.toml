# Bossjob PH:
# This file now points at the real public board host instead of a fake API hostname.
# The current adapter still expects Bossjob's JSON transport shape, so keep this disabled
# until the adapter is aligned with the live endpoint you validate in the browser.
name = "bossjob-ph"
kind = "bossjob_ph"
enabled = false
priority = 4
trust_weight = 0.65
fetch_cap = 50
board_url = "https://bossjob.ph/en"
