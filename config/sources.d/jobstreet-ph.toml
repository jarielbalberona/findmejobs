# JobStreet PH:
# This endpoint currently returns HTML/challenge content to this scraper context, not the JSON
# shape expected by `JobStreetPHAdapter` (`data.jobs`/`jobs` list). Keep disabled until adapter
# and headers/session strategy are updated against a verified live transport.
name = "jobstreet-ph"
kind = "jobstreet_ph"
enabled = false
priority = 5
trust_weight = 0.7
fetch_cap = 50
board_url = "https://www.jobstreet.com.ph/api/chalice-search/v4/search?keywords=software%20engineer&locations=Philippines&jobsPerPage=50&pageNumber=1"
