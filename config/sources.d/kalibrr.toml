# Kalibrr:
# Public page is real, but the currently discoverable API transport responds with authorization
# errors for anonymous access and does not match `KalibrrAdapter` expected payload (`jobs` list).
# Keep disabled until parser+fetch strategy are updated and validated end-to-end.
name = "zk-tech-kalibrr"
kind = "kalibrr"
enabled = true
priority = 6
trust_weight = 0.75
fetch_cap = 50
company_name = "ZK Tech I.T. Solutions"
board_url = "https://www.kalibrr.ph/id-ID/c/zk-tech-i-t-solutions/jobs"
