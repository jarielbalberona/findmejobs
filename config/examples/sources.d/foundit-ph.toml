# foundit Philippines:
# This is the real public search surface. The adapter is still written around a JSON jobs payload,
# so keep it disabled until you confirm and lock the current transport URL and shape.
name = "foundit-ph"
kind = "foundit_ph"
enabled = false
priority = 5
trust_weight = 0.7
fetch_cap = 50
board_url = "https://www.foundit.ph/srp/results?query=software%20engineer"
