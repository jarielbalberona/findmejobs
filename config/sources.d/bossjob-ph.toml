# Bossjob PH:
# Public board URL is correct, but accessible `/api/*` paths currently return HTML pages/404 in
# this scraper context and not the adapter payload (`data.jobs`/`jobs`). Keep disabled until
# the ingestion adapter is aligned with a stable, verifiable transport.
name = "bossjob-ph"
kind = "bossjob_ph"
enabled = false
priority = 4
trust_weight = 0.65
fetch_cap = 50
board_url = "https://bossjob.ph/en"
