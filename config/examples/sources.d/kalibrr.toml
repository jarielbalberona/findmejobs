# Kalibrr:
# Kalibrr documents employer-side career-site integration, not a stable public generic search API.
# This points at a real public company jobs page instead of a fake API URL. Keep disabled until
# the adapter is updated to the transport you actually observe and validate.
name = "zk-tech-kalibrr"
kind = "kalibrr"
enabled = false
priority = 6
trust_weight = 0.75
fetch_cap = 50
company_name = "ZK Tech I.T. Solutions"
board_url = "https://www.kalibrr.ph/id-ID/c/zk-tech-i-t-solutions/jobs"
