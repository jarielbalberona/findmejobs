# foundit Philippines:
# Public search surface is correct, but requests are currently redirected through anti-bot gates
# and do not return parser-compatible JSON (`jobs` list). Keep disabled until transport is
# stabilized and the adapter is updated accordingly.
name = "foundit-ph"
kind = "foundit_ph"
enabled = true
priority = 5
trust_weight = 0.7
fetch_cap = 50
board_url = "https://www.foundit.ph/srp/results?query=software%20engineer"
